import styled from 'styled-components';

const Parent = () => {
    return (
        <ParentStyled className='Parent'>
            Parent
        </ParentStyled>
    );
}

export default Parent;

const ParentStyled = styled.div`

`;